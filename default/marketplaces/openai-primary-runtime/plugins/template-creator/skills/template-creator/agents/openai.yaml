interface:
  display_name: "Template Creator"
  short_description: "Create or update personal reference-backed templates"
  default_prompt: "Use $template-creator to create a template from my documents, presentations, spreadsheets, ImageGen or Product Design images, emails, and Slack messages or update the artifact template skill I pass."
policy:
  allow_implicit_invocation: true
