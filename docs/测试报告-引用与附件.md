# codex-ui @ / $ 引用专项测试报告

- 测试日期：2026-08-09
- 被测版本：release（`src-tauri/target/release/codex-ui.exe`，构建于 2026-08-09 05:54，含 `@`/`$` 对齐 VS Code 协议格式功能）
- codex 基线：0.146.0-alpha.9.2（deepseek 提供方）
- 测试方式：vitest 单元/组件测试 + CDP（9222）驱动真实 UI + 3 条真实模型消息；测试会话统一 `[测试@$]` 前缀，结束后删除
- 结论：**@ 单独、$ 单独、@+$ 混合使用均通过**；测试发现并修复 2 个问题（`$` 菜单键盘导航、`$` 技能名冒号触发），修复后 E2E 16/16 通过

---

## 一、自动化基线

`npm test`：11 个文件、83/83 通过（基线 56 个，本次新增 27 个用例）。

| 测试文件 | 覆盖点 | 新增 |
| --- | --- | --- |
| `src/lib/__tests__/mention.spec.ts` | 触发词解析边界（含冒号技能名）、混合附件协议组装、`stripMentionContext` 还原 | +11 |
| `src/components/__tests__/ComposerBar.spec.ts` | 输入→菜单→选择→附件→发送→键盘交互（含 `$` 键盘选中） | +13（新文件） |
| `src/components/__tests__/MessageItem.spec.ts` | 混合回显渲染、技能去重、协议段落不泄漏 | +3 |

另修复 `src/lib/mention.ts`（`$` token 允许冒号）、`src/components/MentionMenu.vue` 与 `src/components/ComposerBar.vue`（`$` 菜单键盘导航），并重建 release 可执行文件。

## 二、真实 UI E2E（CDP + 真实模型）

执行 `node scripts/verify-mentions.mjs`（修复后重建 release 再跑）：16/16 通过。测试临时目录 `%TEMP%\codex-ui-mention-test-*`，引用文件 `hello.txt` 含唯一标记词；截图证据保存在 `%TEMP%\codex-ui-mention-evidence-G6tJ7a\`（8 张，不提交仓库）。

### 核心链路结果

| 场景 | 验证点 | 结果 |
| --- | --- | --- |
| @ 单独 | 选中后出现 `@hello.txt` 附件标签、触发词移除 | 通过 |
| @ 单独 | 回显渲染 `@hello.txt` 标签 | 通过 |
| @ 单独 | 模型回复包含文件标记词（文件可被模型读取） | 通过 |
| $ 单独 | 选中后出现 `$csharp-code-rules` 附件标签、触发词移除 | 通过 |
| $ 单独 | **修复验证**：Enter 选中技能而未发送消息 | 通过 |
| $ 单独 | 回显渲染 `$csharp-code-rules` 标签 | 通过 |
| $ 单独 | 模型回复体现技能内容（SKILL.md 注入生效，回复 XML 注释规则原文） | 通过 |
| @+$ 混合 | 同一消息同时挂两个附件标签 | 通过 |
| @+$ 混合 | 回显同时渲染 `@hello.txt` 与 `$csharp-code-rules` | 通过 |
| @+$ 混合 | 模型回复同时覆盖文件标记词与技能名称 | 通过 |

### UI 边界抽查（不发真实模型）

| 场景 | 结果 |
| --- | --- |
| 词中 `@`（前面无空格）不弹菜单 | 通过 |
| **修复验证**：`$ida-pro-mcp:idapython` 整名触发菜单并命中 | 通过 |
| `@` 空 token 显示「选择文件…/选择文件夹…」本地选择行 | 通过 |
| Esc 关闭菜单 | 通过 |
| 仅附件无文本时发送按钮可用 | 通过 |
| 附件标签可移除 | 通过 |

## 三、本次修复

1. **【中】`$` 菜单补键盘导航，与 `@` 一致**：`MentionMenu.vue` 的技能列表增加高亮态（hover/↑↓ 同步），`selectHighlighted` 对 `$` 分支选中高亮技能；`ComposerBar.vue` 的 Enter/↑↓ 拦截从仅 `@` 扩展为 `@`/`$` 通用。修复前 Enter 会直接发送原始文本，修复后 Enter 选中技能并生成附件（E2E 断言消息数 1→1、组件测试覆盖 Enter 与 ↑↓ 选中）。
2. **【中】`$` 触发词放开冒号**：`matchMentionToken` 改为按类型校验字符集——`$` 允许冒号（`$ida-pro-mcp:idapython` 可整名触发），`@` 仍不允许（Windows 文件名不含冒号，语义不变）。E2E 实测 22 个插件技能可整名触发。
3. **【低】测试方法学要点（非产品缺陷）**：E2E 启动应用必须可见窗口；`Start-Process -WindowStyle Hidden` / `windowsHide: true` 会阻止 WebView2 初始化，远程调试端口不开放。已在 `verify-mentions.mjs` 中固定为可见启动。

### 保留的设计限制

- 触发词必须是文本末尾的完整 token，输入空格后菜单即关闭：`@my file.txt`（文件名含空格）无法整名触发，需用本地文件选择器。这是 mention 触发语法的既有语义（空格结束触发词），本次不改动。

## 四、已核实的设计行为（非缺陷）

- `@` 必须位于行首或空格后（`看下@file` 不触发），为 `matchMentionToken` 既有设计。
- 发送格式与 `docs/引用与附件.md` 一致：文件段 `# Files mentioned by the user:` + `## My request:` + 技能链接 + 结构化 skill 项；路径统一正斜杠；图片保留为 `localImage` 项。
- 同一条消息含结构化 skill 项时，文本技能链接不重复渲染（去重生效）。
- `@` 空 token 时 Enter 打开本地文件选择器（组件测试覆盖）。

## 五、验证与清理

- 修复后重建：`npm run build`（含 vue-tsc 类型检查）+ `cargo build --release`，E2E 针对新二进制 16/16 通过。

- 测试临时目录、测试会话文件（含标记词）已删除；应用进程已退出、9222 端口已释放；截图证据保留在 `%TEMP%` 供查看，未提交仓库。
- 变更：`src/lib/mention.ts`、`src/components/MentionMenu.vue`、`src/components/ComposerBar.vue`、三个测试文件、`scripts/verify-mentions.mjs`、`dist/`（重建）、`src-tauri/target/release/codex-ui.exe`（重建）、本报告。

## 六、对话渲染改进回归（2026-08-09）

实施「对话内容 UI 渲染改进」三阶段方案后回归：`npm test` 18 个文件、129/129 通过；Rust 单测 15 个（diff 解析/重建/建行）+ 集成 2 个通过；`scripts/verify-mentions.mjs` 扩展至 27 项（新增代码块渲染、链接分发、文件变更 diff 与独立窗口抽查）全部通过；`scripts/check-mode-lock.mjs` PASS。

### 实施内容

- **流式性能**：`useThrottledRef` 让助手消息/思考过程/工具输出在流式期间最多每 80ms 刷新一次，结束立即刷净；工具输出渲染截断为末尾 5000 行并提示；修复“进行中进行中”状态文本重复。
- **代码高亮**：接入 highlight.js（离线打包，约 20 种常用语言），代码块显示语言徽标与语法高亮，保留复制按钮；顺带修复 DOMPurify 剥离文档根元素 `<pre>` 的隐患（消息以代码块开头时此前会失去 pre/复制按钮，现包一层 div 再 sanitize）。
- **信息层次**：消息时间戳（HH:mm）与跨天日期分隔线；思考过程完整展示（不限高）；未知消息类型友好提示 + 原始数据展开。
- **交互**：图片灯箱与加载失败占位；助手最终答复复制、工具命令复制；折叠/复制按钮补 aria 属性。
- **小缺陷**：错误 toast 统一提取 `error.error.message / error.message`；队列模式下回合中发送弹“已加入队列”提示。

### 追加改动（2026-08-09 第二轮）

- 思考过程去掉 320px 限高与“展开全部”交互，内容完整展示（保留节流、折叠、耗时与 aria）。
- 对话链接按类型打开：http/https 用默认浏览器；本地文件/目录（`file:///`、`/D:/`、`D:/`、`D:\`、相对路径按工作目录解析）新增 Rust `reveal_path` 命令用 `explorer /select` 定位；右键“打开链接”同步该行为。
- 顺带修复：DOMPurify 默认 URI 白名单不含 `file:`，导致本地文件链接 href 一直被剥离（真实应用此前 file 链接不可点）；现放行 `file:`。
- E2E 新增链接分发抽查：通过应用内置测试钩子（`__CODEX_UI_TEST__`）记录 `openLink` 分发，断言网页链接调 `open_url`、本地链接调 `reveal_path`，不真实弹出浏览器/资源管理器。

### 追加改动（2026-08-09 第三轮）

- 修复 Markdown 盘符路径链接：DOMPurify URI 白名单补充 `[a-z]:(?:[\\/]|%5[cC]|%2[fF])`，`[计划.md](D:\codex\...\计划.md)` 这类链接（含 marked 编码的 `%5C`）不再被剥离，点击沿用网页/本地分流（本地经 `reveal_path` 定位）。
- 用户消息气泡简化：去掉悬停“复制/重试”按钮与时间戳；助手最终答复的复制与时间戳保留。
- E2E 本地链接抽查增加裸盘符路径形式（`C:/...`），断言 href 保留且 `reveal_path` 收到反斜杠归一化路径。

### 追加改动（2026-08-09 第四轮）

- 文件变更卡：点击文件路径/名称行弹出大尺寸预览弹窗（约 92vw × 86vh），完整展示该文件统一 diff（新增/删除/上下文行着色、可滚动），不再受卡片内 120/260px 截断；× / Esc / 点击遮罩关闭，无 diff 的行不可点击。
- 新增 5 个 ToolCard 预览用例（含完整展示、遮罩/Esc/× 关闭、无 diff 不弹窗）。

### 追加改动（2026-08-09 第五轮）

- 文件变更 diff 预览升级为单栏内联完整文本对比（VS Code 风格）：全文件内容、双行号列（旧 | 新）、未变行灰色、旧行红色在上、新行绿色在下、变更处“旧 | 新”分隔。
- 数据方案：新增 Rust `read_file` 命令读取新内容（仅文本/UTF-8），旧内容由 unified diff 反向重建（`src/lib/diff.ts`：parse/applyReverse/buildInlineRows）；二进制、读取失败或格式异常回退完整统一 diff。
- 修复：diff 尾部换行被误解析为空 ctx 行导致 hunk 计数不一致（解析时跳过空行）。
- 新增 `diff.spec.ts`（10 用例）、`FileDiffPreview.spec.ts`（6 用例）、Rust `read_file` 单测（2 用例）；E2E 新增真实 apply_patch 场景（模型修改 hello.txt → 点击变更行 → 断言旧行/新行/“旧 | 新”分隔）。

### 追加改动（2026-08-10 第六轮）

- diff 预览改为**独立 Tauri 新窗口**（label=`diff-preview`，原生可调整大小，工具栏含全屏/关闭）；窗口按 label 识别，参数经 Rust 共享状态 `take_diff_params` 一次性传递；修复同步命令创建 WebView2 窗口导致的主线程死锁（改为 `run_on_main_thread` 异步创建）。
- diff 解析迁移到 **Rust**（`src-tauri/src/codex/diff.rs`）：`build_diff_preview` 一次 IPC 完成路径解析 + 读文件 + unified diff 反向重建 + 内联行生成；删除 JS `diff.ts` 与 `read_file` 命令。
- 新增**滚动条式变更位置指示条**（`.diff-minimap`）：删除行红段、新增行绿段，按行位置百分比定位。
- 新增 `DiffWindowView.spec.ts`（6 用例）与 Rust diff 单测（15 用例）；E2E 场景 6 改造为多窗口（CDP 轮询第二个页面目标，断言内联行与 minimap，关闭新窗口），共 27 项全过。

### 追加改动（2026-08-10 第七轮）

- diff 窗体修正：移除“全屏/关闭”按钮与 Esc 关闭（仅原生标题栏关闭）；窗口默认最大化打开；指示条改为测量驱动（按行 `offsetTop/内容总高` 定位，`ResizeObserver` 重算），与真实变更位置对齐；diff 窗口接入与主窗口一致的自定义右键菜单（抽取 `useContextMenu` 共用，阻止默认菜单不可刷新）。
- 代码语法高亮：`languageFromPath` 按扩展名识别语言（约 20+ 种），diff 行用 highlight.js 逐行高亮（红/绿背景与词法着色并存，未知语言/超 2 万行回退纯文本）；窗口复用改为“更新参数 + reload”，避免同 label 窗口竞态。
- 新增精简 E2E `scripts/verify-diff.mjs`：1 条真实模型消息（apply_patch 改 hello.txt + 新建 sample.ts），8 项断言约 14 秒完成（窗口打开、内联旧/新/分隔、指示条对齐、语法高亮、右键菜单），作为 diff 功能快速验证路径；`verify-mentions.mjs`（31 项）保留作全量回归。
- 修复两处实测问题：指示条由“文档比例”改为“滚动条拇指比例”（`内容偏移 / (scrollHeight - clientHeight)`，300 行文件下与滚动条精确对齐）；右键菜单在非编辑区无选区时始终提供“复制”（复制整行），diff 窗口 `useContextMenu(true, true)` 启用。

### 追加改动（2026-08-10 第八轮）

- diff 窗口**移除右键菜单**：右键仅阻止默认（不出现“刷新”等默认菜单），不显示任何自定义菜单；主窗口右键菜单保持不变。
- **移除滚动条指示色功能**：经多次实现（文档比例、滚动条拇指比例、自绘滚动条）仍无法与原生滚动条精确对齐，按用户要求去掉变色指示段，**恢复原生滚动条**（纵向/横向均原生）。

### 追加改动（2026-08-10 第九轮）

- **Markdown 渲染上 Web Worker**：`marked.parse` 离屏执行（`markdown.worker.ts`），主线程只做 DOMPurify 清洗与 DOM 装饰（复制/链接/hljs 高亮）；Worker 不可用/超时/出错自动回退主线程同步解析，流式期间按最新请求丢弃乱序旧结果。
- **ChatView 超长会话虚拟化**：手写测量式虚拟列表（`virtualList.ts` 纯函数 + 行高 ResizeObserver 测量缓存），只渲染可视区 ±5 行，总高 spacer + 绝对定位；保留流式吸底、回到底部、日期分隔；历史整批加载时清空测量缓存。
- 新增 `markdownRenderer.spec.ts`（4 用例）、`virtualList.spec.ts`（5 用例）、ChatView 虚拟化结构用例；E2E 场景 4（真实 Worker 代码块渲染）与主流程全过。

### 追加改动（2026-08-10 第十轮）

- **Markdown 流式增量渲染**：`MarkdownText` 去掉整段 `v-html` 替换，改为增量 DOM 更新——新 HTML 以旧为前缀时直接追加尾部；否则按顶层节点做公共前缀匹配，保留未变化节点（选区/高亮/复制按钮不被打断），仅替换尾部节点；代码块闭合等结构重排时整段替换兜底。
- 新增增量渲染单测（追加保留前面块 DOM 引用、代码块闭合回退整段替换）；`npm test` 149/149，E2E 30/30 + verify-diff 7/7 + check-mode-lock 全过。

### 追加改动（2026-08-10 第十一轮）

- **进行中计数增量维护**：`useCodex` 新增 `activeWorkByThread`，`upsertItem`/流式 delta 维护每线程计数，`ChatView` 的 `hasActiveWork` 改为 O(1) 读取，去掉超长会话的全量扫描；历史加载时按线程重算。
- **长串断行**：`.msg-agent .md` 增加 `overflow-wrap: anywhere`，超长 URL/无空格串不再横向撑出。
- **图片懒加载**：用户附件图片与 imageView 加 `loading="lazy"` + `decoding="async"`。
- 新增 MessageItem 懒加载断言与 ChatView 思考中提示计数用例；`npm test` 150/150，E2E 30/30 + verify-diff 7/7 + check-mode-lock 全过。

### 追加改动（2026-08-10 第十二轮）

- **ToolCard 命令输出增量渲染**：`.tool-output` 去掉整段 `v-html`；`ansi.ts` 新增 `ansiToHtmlWithState`（携带样式状态、返回结束样式），ToolCard 只在已渲染文本之后解析新增尾部并 `insertAdjacentHTML` 追加；未完成的 `\x1b` 转义序列暂缓至完整；条目替换/文本回退/截断切换时重置整段重渲染。
- 新增 ansi 状态延续与 ToolCard 增量追加/重置用例；`npm test` 155/155，E2E 30/30 + verify-diff 7/7 + check-mode-lock 全过。

### E2E 新增抽查

| 场景 | 结果 |
| --- | --- |
| 代码块保留 `<pre>` 且有语法高亮（真实回复，模型输出 javascript 代码块） | 通过 |
| 代码块有复制按钮与语言徽标 | 通过 |
