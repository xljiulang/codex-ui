# 前端命名语义漂移清单

> 2026-09-05 全库审计产出：梳理「功能/概念的描述历史上发生过更变，但代码里的命名语义没跟着更新」的项，按必要性分三级。
> 评审基准：README.md 与 docs/前端模块地图.md 的当前描述 + git 提交历史（如 0d26dd7 History→Session、4eebed3 任务模式→协作模式、3babf78 thinking→waiting、784d8a8 编辑器并入标签页）。
> 状态标注：✅ 已处理（2026-09-05 重构）/ 📌 仅记录（供后续处理）。

## P0 明显误导（名字/注释会直接把人带偏）——8 项，全部 ✅

| # | 位置 | 现名 → 处理 | 说明 |
|---|---|---|---|
| 1 | `useEditorTabs/close.ts` | `closeAllOtherTabs` → **删除**（连带私有 `closeTabsMatching`） | 名说「关闭其它标签」（编辑器惯例=保留活动标签），实现 `closeTabsMatching(() => true)` 连活动标签一起关；UI 零调用，仅测试引用（已改挂存活的 `closeAllTabs`） |
| 2 | `useCodex/actions.ts` | `ensureThreadLoaded` → `ensureThreadResumed` | 函数体只做 `thread_resume` + 写 `resumedThreadId`，不加载任何消息项（加载在 `loadThreadInto`） |
| 3 | `SettingsView.vue` | 分区 id `memory` → `basic`（类 `settings-section-memory` → `-basic`） | 该分区 label 早已是「基础设置」聚合区（codex 路径/终端 Shell/默认权限/跟进方式/记忆都在内），f07ad5b 只改了 label 没改 id；watch 条件与测试选择器同步 |
| 4 | `useCodex/types.ts` | `SessionTab` 接口注释重写 | 注释描述已删除的「store current\* 投影」架构（store 已无任何 current\* 字段），照注释找状态会找错地方；改为「标签对象即唯一事实源」 |
| 5 | `useSessionFs/watcher.ts` | `setSessionFsActive` 注释修正 | 旧注释「切走时停止监听」与实现相反：监听已常驻（`syncWatcher` 跟随工作区），`active` 只区分事件回调里是否刷树 |
| 6 | `useSessionFs/state.ts` | `resetTree` 注释修正 | 注释提及的「剪贴板状态」是已整体移除的应用内剪贴板（改写系统 CF_HDROP），函数亦无对应逻辑 |
| 7 | `src/lib/__tests__/history.spec.ts` | → `src/composables/__tests__/useCodex.sortThreads.spec.ts` | 内容是 `composables/useCodex` 的 `sortThreads` 测试，文件名停在 history 时代且位置不在 lib（lib 下并无 history.ts） |
| 8 | `src/styles/`（8 个文件） | 16 处 0 引用死 CSS 类删除 | `.new-chat-btn`(layout)、`.editor-tab-brand`(tabs)、`.memory-mode-select`/`.server-logs`/`.auth-banner`/`.settings-page-foot`/`.settings-card-desc`/`.model-config-warning`/`.model-config-path-status`/`.skill-status`(settings，含共享选择器组内行)、`.mention-chip`/`.md table .md-caption`(chat)、`.usage-menu-model`(composer)、`.menu-search`/`.option-btn .model-slug`(overlays)、`.git-remote-empty`(git)、`.text-editor-loading`(render)；均为对应 UI 删除/改版后的孤儿选择器，删除前逐类 grep 复核 0 引用 |

## P1 轻度不准确（名实有偏差但不改变行为理解）——14 项，全部 ✅

| # | 位置 | 现名 → 新名 | 说明 |
|---|---|---|---|
| 1 | `useCodex/threads.ts` | `togglePin` → `setThreadPinned` | 实为显式 setter（第二参传目标态布尔、失败回滚），非 toggle |
| 2 | `useCodex/sessionState.ts` | `syncActiveSessionProjection` → `trackActiveSessionTabId` | 「投影」是 store 会话字段移除前旧机制残留，现仅维护最近会话标签 id |
| 3 | `useCodex/capabilities.ts` | 文件名 → `pinnedSection.ts` | 能力探测（codex_pin/title_helper capability）已随 0.149 适配整体删除，文件实际只管置顶分区与排序 |
| 4 | `useCodex/turnControl.ts` | `continueTurn`/`continueTurnForTab` → `startTurn`/`startTurnForTab` | 实际执行 `turn_start` 发起新回合（含恢复），「在当前回合上追加输入」语义属同文件 `steerTurn` |
| 5 | `useCodex/types.ts` 等 | `threadTokenUsage.used` → `contextUsed` | `used` 存最近一次请求的上下文占用（tokenUsage.last），与同对象会话累计字段（input/output/totalTokens…）并排，易误读为累计已用 |
| 6 | `useCodex/types.ts` 等 | `SessionTab.origin` 值 `"session"` → `"history"` | 实义「来自历史会话列表」，0d26dd7 改名时被机械连带改成了 `"session"`（与「标签本身是会话」自指易混）；已确认未持久化，类型补注释 |
| 7 | `InlineInteraction.vue` | `isReviewApproval` → `isOtherApproval` | 实为非命令类审批（文件变更/权限），与 "review" 概念无关 |
| 8 | `lib/permissions.ts` 等 | `toSandboxPolicy` 删除 `_workspace` 死参数；连带 `buildTurnParams`/`buildUserTurn` 删除已死化的 `cwd` 参数 | writableRoots 已固定为空（f20dc00），工作区参数不再参与沙箱策略；调用点与测试同步 |
| 9 | `lib/mention.ts` | `stripSkillLinks` → `stripLeadingRefLinks` | 正则同时剥离 `@`（文件/插件）与 `$`（技能）开头链接，名窄于实 |
| 10 | `EditorPane.vue` + `feedback.css` | `.no-session-state`/`.no-session-hint` → `.no-tabs-state`/`.no-tabs-hint` | 展示的是「当前还没有任何打开的项」（全部标签类型为 0），不止「无会话」 |
| 11 | `EditorPane.vue` + `render.css` | 脏标签关闭确认浮层 `text-editor-overlay/-confirm/-btn` → `tab-confirm-overlay/-confirm/-btn` | 确认流程覆盖 File+Docx 两种脏标签，非文本编辑器专属 |
| 12 | `GitHistoryList.vue` | prop `reloadKey` → `statusSignal` | 类型是 `GitStatus \| null`，当变更信号用，不是 key |
| 13 | `MentionMenu.vue` + `ComposerBar.vue` | 事件 `select-attachment` → `select-file`（处理器 `onSelectFile`） | 仅 `kind === "file"` 行 emit；插件/技能走内联引用 chip，不进附件区 |
| 14 | DiffPane/TextEditorPane/DocxEditorPane + 5 个 css | `diff-window*`/`text-editor-window`/`docx-editor-window` → `*-pane*` | 784d8a8 后组件已并入主窗口标签页（组件名早已是 \*Pane），「window」不再指窗口 |

## chat 词簇（0d26dd7 History→Session 改名的漏网）——✅ 已彻底统一

- `TabKind.Chat`/`TabIcon.Chat` → `TabKind.Session`/`TabIcon.Session`，值 `"chat"` → `"session"`（约 34 文件 169 行，含测试 fixture 与字面量；已确认 kind/icon 值不持久化到 sessions.json，无迁移风险）。
- 函数/字段：`newChat` → `startNewSession`（在线程未建时发起首回合）、`newEmptyChat` → `newEmptySession`、`resetToNewChat` → `resetToNewSession`、`creatingChat` → `creatingSession`、`newChatWorkspace` → `newSessionWorkspace`。
- **有意保留**：`ChatView.vue` 与根类 `.chat`（消息流「聊天界面」是现名非旧名）、`wire_api` 的 `"chat"` 协议选项（OpenAI chat completions）、WeChat（微信）词根。

## P2 仅风格项 —— 1~4 已处理（✅ 2026-09-05 P2 批次），5~8 维持现状

1. ✅ `CtxItem` 同名双定义 → 已改名区分：`useActionMenu.ts` 的富菜单项类型改为 `ActionMenuItem`（10 个使用方同步，含渲染层 `ContextMenu.vue` 的 props 类型）；`useContextMenu.ts` 的全局复制/链接菜单项保留唯一 `CtxItem`。
2. ✅ `useResourceDragDrop.ts` 的 `isDragging` → `isDragInteraction`（`dragActive || dragStart !== null` 含按下未超 5px 阈值阶段，非严格拖拽中；`ResourceView.vue` 解构与测试同步）。`RightPanel.vue` 同名本地 ref 是面板宽度拖拽、语义准确，未动。
3. ✅ `useEditorTabs/refresh.ts` 的 `refreshActiveTabFromFs` → `refreshTabsFromFs`（复数覆盖全部命中标签：先标记 stale、再刷新活动标签；`useEditorTabs/index.ts` 导出与 `useSessionFs/watcher.ts` 调用点同步）。
4. ✅ `ComposerBar.vue` 的 `submit(flip)` / `useCodex/actions.ts` 的 `sendPrompt(text, flip)` → `invertFollowup`（Ctrl+Enter 对单条消息反转跟进处理方式 adjust↔queue）。
5. 📌 维持现状：同一会话列表域两套词根并存——协议词根 `threads`/`refreshThreads`/`threadTitle` 与 UI 词根 `loadingSessions`/`PanelTab "session"`。约定已写入模块地图（thread=协议词、session=UI 词），代码不改。
6. 📌 维持现状：`WechatBindDialog.vue` 的 prop `:thread`（`ThreadSummary` 协议类型）与「会话」基准词不一致——协议命名可接受。
7. 📌 维持现状：ToolCard 的 `todoList`（协议 item 类型名）与 UI 词「计划」并存——协议名保留。
8. 📌 流程约定：提交信息 scope 与模块划分不一致（`chat` 混用消息流/输入区、`nav` 实指回合导航、`ui` 跨模块、`设置`/`settings` 中英混用）——建议后续提交按模块地图的模块划分子 scope。

## 核对过、确认无残留的高风险点（勿重复处理）

- `thinking→waiting`（3babf78）：src 内无 thinking 残留（`ICON_THINK` 属推理块语义，另一有效概念）。
- `TaskMode→CollaborationMode`（4eebed3）：代码侧无 taskMode/executionMode 残留；`events.ts` 的 `"task-mode-reconcile"` 是服务端协议事件名，保留正确。
- 欢迎标签（bbc6a03 移除）、导航底栏/底边进度条（2cb8d35 移除）：无孤儿代码残留。
- 合理保留的 history 词：协议字段 `historyMode`、Git 提交历史（`GitHistoryList`）、输入历史（`useInputHistory`）。
- `MemoryConfigState.allowToolGenerate` ↔ 配置键 `disable_on_external_context` 的映射语义已由维护者亲自核对确认，不列项。
